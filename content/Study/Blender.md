1.2 让手听话
控制： 鼠标中键 移动自由视角（右上角轮盘效果一致），shift+中键 平移视角  ，滚轮缩放视图
模型缩放（点击后按s），X（删除），shift+A新建物体。移动（G，XYZ键确定XYZ轴），右键可退出（移动，缩放状态），旋转（R），alt+ r/s/g（撤销旋转或缩放），隐藏（H），shift+H（隐藏未被选中物体）。shift+D（移动并复制），~ （视图选择）
1.3 认识界面 
