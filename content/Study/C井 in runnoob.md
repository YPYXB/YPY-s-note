#简介
重要功能：布尔条件，自动垃圾回收，标准库，组件版本，属性和事件，委托和事件管理，泛型，索引器，条件编译，简单的多线程，LINQ和LAMbda表达式，集成windows。
作为C#的入门学习。

#程序结构

- 命名空间声明（Namespace declaration）
-  一个 class
- Class 方法
- Class 属性
- 一个 Main 方法
- 语句（Statements）& 表达式（Expressions）
- 注释
大小写敏感。

---
#基本语法
有namespace ，namespace中有很多class，在其中某个class里写main函数。
	using System;一般为第一条语句，可以有多个using语句。
	//为注释，/*为多行注释
标识符：以字母、下划线或 @ 开头，后面可以跟一系列的字母、数字（ 0 - 9 ）、下划线（ _ ）、@。第一个不能为数字

里面不能有符号
顶级语句 
{
无需类或方法：顶级语句允许你直接在文件的顶层编写代码，无需定义类或方法。
- **文件作为入口点**：包含顶级语句的文件被视为程序的入口点，类似于 C# 之前的 `Main` 方法。
    
- **自动 `Main` 方法**：编译器会自动生成一个 `Main` 方法，并将顶级语句作为 `Main` 方法的主体。
    
- **支持局部函数**：尽管不需要定义类，但顶级语句的文件中仍然可以定义局部函数。
    
- **更好的可读性**：对于简单的脚本或工具，顶级语句提供了更好的可读性和简洁性。
    
- **适用于小型项目**：顶级语句非常适合小型项目或脚本，可以快速编写和运行代码。
    
- **与现有代码兼容**：顶级语句可以与现有的 C# 代码库一起使用，不会影响现有代码。
}//一个项目中，仅能有一个文件使用顶级语句。

---
#数据类型
三种类型：值类型，引用类型，指针类型
引用类型：内置的有object，Dynamic，string；
object：基类，装箱；值类转换为对象类型，拆箱：变为值类。装过箱才能拆箱。
dynamic：在运行时进行类型检查。
string：“”和@“”
指针类型：Pointer types。
type* identifier；

---
#类型转换
 Convert 类、Parse 方法和 TryParse方法
 隐式类型转换：小类型转换到大类型，不需要显式转换。
 显式类型转换：大转小，需要手动强制转换。
 C#还有指定的类型转换方法。
 Convert：静态方法，在基本数据类型间下载。
 Parse：字符串转换为对应类型，失败抛出异常。
 TryParse：不抛出异常，但是返回一个布尔值。

---
#变量
- 变量名可以包含字母、数字和下划线。
- 变量名必须以字母或下划线开头。
- 变量名区分大小写。
- 避免使用 C# 的关键字作为变量名。
ReadLine()用于接受用户输入。

---
#变量作用域
局部：方法，循环，条件内
块级：在{}内
方法参数：方法当中

---
#运算符
&：全一为一    |：遇一位一  ^：相同为0 ~：取反
is：if（F is char）

---
#判断
 ? :              switch   嵌套switch
 #循环
 foreach (var item in collection)
{
    // 循环
}
#封装
**封装** 被定义为"把一个或多个项目封闭在一个物理的或者逻辑的包中"。在面向对象程序设计方法论中，封装是为了防止对实现细节的访问。
抽象和封装是面向对象程序设计的相关特性。抽象允许相关信息可视化，封装则使开发者_实现所需级别的抽象_。
C# 封装根据具体的需要，设置使用者的访问权限，并通过 **访问修饰符** 来实现。
C# 支持的访问修饰符如下所示：
- public：所有对象都可以访问；
- private：对象本身在对象内部可以访问；
- protected：只有该类对象及其子类对象可以访问
- internal：同一个程序集的对象可以访问；
- protected internal：访问限于当前程序集或派生自包含类的类型。
#方法
方法（Method）是将一组相关语句组织在一起、用来执行特定任务的代码块。它是 C# 中实现**代码复用**和**逻辑封装**的核心机制。每一个 C# 程序至少有一个 `Main` 方法作为程序入口。
使用方法需要两步：**定义方法**和**调用方法**。
<访问修饰符> <返回类型> <方法名>(<参数列表>)
{
    方法体
}
|方式|关键字|行为|典型场景|
|---|---|---|---|
|按值传递|（无）|复制参数值，方法内修改不影响原变量|大多数方法调用（默认）|
|按引用传递|`ref`|传递变量的引用，方法内修改会改变原变量|需要修改外部变量（如交换值）|
|输出参数|`out`|类似 ref，但方法必须赋值，调用前无需初始化|方法需要返回多个值|
#可空类型（Nullable）
